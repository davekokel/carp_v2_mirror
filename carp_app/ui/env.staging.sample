# Sample only — do not put real creds here
ENV_NAME="staging"

# Use either CONN (full DSN) set in Streamlit Secrets, OR the PG* parts below.
# We intentionally omit a DSN here to avoid secret scanners tripping on examples.

# PG connection parts:
PGHOST="aws-1-us-west-1.pooler.supabase.com"
PGPORT="6543"
PGUSER="postgres.zebzrvjbalhazztvhhcm"
PGPASSWORD="<set-in-cloud-or-local-secrets>"
PGDATABASE="postgres"
