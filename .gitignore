# ─────────────────────────────────────────────────────────────────────────────
# Environment files
# ─────────────────────────────────────────────────────────────────────────────
.env*
!.env.example
!.env.*.example

# Explicit local env variants (kept ignored)
.env.staging.direct
.env.staging.pooler

# ─────────────────────────────────────────────────────────────────────────────
# Local artifacts / snapshots
# ─────────────────────────────────────────────────────────────────────────────
snapshots_*/
supabase/snapshots/
snapshots/*_data_*.sql

# If you commit selected staging snapshots:
!snapshots_staging/*.sql

# ─────────────────────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/

# ─────────────────────────────────────────────────────────────────────────────
# Streamlit
# ─────────────────────────────────────────────────────────────────────────────
.streamlit/secrets.toml

# ─────────────────────────────────────────────────────────────────────────────
# Build / dist
# ─────────────────────────────────────────────────────────────────────────────
dist/
dist/*.zip

# ─────────────────────────────────────────────────────────────────────────────
# OS/editor cruft
# ─────────────────────────────────────────────────────────────────────────────
.DS_Store
_trufflehog_local.json

# ─────────────────────────────────────────────────────────────────────────────
# ERD artifacts
# ─────────────────────────────────────────────────────────────────────────────
supabase/erd/

# ─────────────────────────────────────────────────────────────────────────────
# Supabase migrations
# (Allow migrations by default; archive/backup folders are ignored)
# ─────────────────────────────────────────────────────────────────────────────
!supabase/migrations/*.sql
!supabase/migrations/**/*.sql
supabase/migrations/_archive/**
supabase/migrations/_archive_legacy/**

# ─────────────────────────────────────────────────────────────────────────────
# Backup files
# ─────────────────────────────────────────────────────────────────────────────
*.bak*
*.sql.gz

# ─────────────────────────────────────────────────────────────────────────────
# Docs / misc
# ─────────────────────────────────────────────────────────────────────────────
docs/DB Connection Playbook v2*

# ─────────────────────────────────────────────────────────────────────────────
# Seed kit artifacts (temp Office files, zips, dated dirs)
# ─────────────────────────────────────────────────────────────────────────────
carp_app/seed_kits/**/~$*.xlsx
carp_app/seed_kits/**/~*.xlsx
carp_app/seed_kits/**/*.zip
carp_app/seed_kits/**/20*/
carp_app/seed_kits/**/seed_kit_*/

# ─────────────────────────────────────────────────────────────────────────────
# One-off CSV exports (local audits)
# ─────────────────────────────────────────────────────────────────────────────
public_*_*.csv
crosses/
fish/
plates/
transgene_alleles/
supabase/baselines/
schema_implied/
schema_real/

# local tooling
/tools/


# legacy wrangling v3 outputs
seed_kits/legacy_wrangling_v3/working/

# local working artifacts
seed_kits/legacy_wrangling_v2/working/Unique_parent_names__mom_dad_combined__preview_dqm_from_raw.csv

# Excel temp files
~$*.xlsx

seed_kits/legacy_wrangling_v3/working/exp_treatment_manual_overrides.csv

# legacy imaging: huge filesystem scan artifacts (do not commit)
seed_kits/legacy_wrangling_v4/raw/foundation_tiff_files_*.txt
seed_kitsseed_kits/legacy_wrangling_v4/raw/foundation_tiff_files_*.txt.gz

# legacy imaging v4: generated working/QC artifacts (do not commit)
seed_kits/legacy_wrangling_v4/working/
seed_kits/legacy_wrangling_v4/qc/
seed_kits/legacy_wrangling_v4/raw/foundation_tiff_files_*.txt
seed_kits/legacy_wrangling_v4/raw/foundation_tiff_files_*.txt.gz
