# streamlit_app.py
import streamlit as st
st.set_page_config(page_title="ping", layout="wide")
st.title("✅ Streamlit is rendering")
st.write("If you can read this, the runtime is alive and the Main file path is correct.")
# ping
